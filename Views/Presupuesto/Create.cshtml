@{
    ViewData["Title"] = "Crear Presupuesto";
}
@using TP6MVC.Models
@model Presupuesto

<div class="container mt-5">
    <h2 class="text-center mb-4">Crear Presupuesto</h2>

    <form asp-controller="Presupuesto" asp-action="Create" class="border p-4 shadow-sm rounded">
        <div class="form-group mb-3">
            <label asp-for="NombreDestinario" class="form-label fw-bold">Nombre</label>
            <input asp-for="NombreDestinario" class="form-control" placeholder="Ingrese el nombre del destinario" />
            <span asp-validation-for="NombreDestinario" class="text-danger"></span>
        </div>

        <div class="text-center">
            <button type="submit" class="btn btn-primary">Enviar</button>
        </div>
    </form>
</div>